name: dev_actions

on:
  push:
    branches: [ dev ]
  pull_request:
    branches: [ dev ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.301
    - name: Caching
      id: cache
      uses: actions/cache@v2
      with:
        path: ~/.nuget/packages
        key: ${{ runner.os }}-nuget-${{ hashFiles('**/packages.lock.json') }}
        restore-keys: ${{ runner.os }}-nuget-   
    - name: Restore dependencies
      if: steps.cache.outputs.cache-hit != 'true'
      run: dotnet restore
    - name: Build
      run: dotnet build --no-restore
    - name: publish
      run: dotnet publish --no-build 
    - name: artifact_upload
      uses: actions/upload-artifact@v2
      with:
        name: generate_build_artifact
        path: /home/runner/work/CI_CD_training/CI_CD_training/Ci_Cd_DemoApp/bin/Debug/netcoreapp3.1/publish/
    - name: Deploy Application to Live  
      shell: cmd  
      run: '"C:/Program Files (x86)/IIS/Microsoft Web Deploy V3/msdeploy.exe" -source:package="/home/runner/work/CI_CD_training/CI_CD_training/Ci_Cd_DemoApp/bin/Debug/netcoreapp3.1/publish/generate_build_artifact.zip" -dest:auto,computerName="https://3.22.68.224:8172/msdeploy.axd?site=Mysite",username="EC2AMAZ-DHHL9I7\Administrator",password="!mnQ9&;L-eRANNTX%*k.ogNBVIR84CYB",authtype="Basic",includeAcls="False" -verb:sync -disableLink:AppPoolExtension -disableLink:ContentExtension -disableLink:CertificateExtension -allowUntrusted'
